# NOT currently referenced by `talosctl gen config` (see
# ensure_base_talos_config() in scripts/lib/common.sh) — an empty/no-op
# --config-patch-worker file was unreliable across talosctl versions
# (confirmed to fail two different ways, see docu/11-troubleshooting.md and
# https://github.com/siderolabs/talos/issues/13029), so the flag is omitted
# entirely while this file has nothing real to contribute.
#
# To use this again: add real YAML content below, then add
# `--config-patch-worker "@${REPO_ROOT}/talos/patches/worker.yaml"` back to
# the `talosctl gen config` call in ensure_base_talos_config().
